{
  "name": "telegram-ai-assistant",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "wrangler build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env development",
    "db:migrate": "wrangler d1 migrations apply telegram-ai-bot --env production",
    "db:migrate:dev": "wrangler d1 migrations apply telegram-ai-bot-dev --env development --remote"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0",
    "wrangler": "^4.40.2"
  },
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}
